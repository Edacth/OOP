#include <iostream>
#include <String>
#include <fstream>
#include "Text Files.h"
using namespace std;

int main()
{
	/*
	std::fstream file;

	file.open("test.txt", std::ios::out);

	if (!file.is_open())
	{
		std::cout << "File not found." << std::endl;
		return -1;
	}

	file << "Test lol" << std::endl;
	*/
	

	//printer();
	//saver();
	//garble();
	//commaSep();
	//loadData();
	caeserMessage();

	return 0;
}